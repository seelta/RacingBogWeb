import { DTOMensualidadJugador } from './dtomensualidad-jugador';

describe('DTOMensualidadJugador', () => {
  it('should create an instance', () => {
    expect(new DTOMensualidadJugador()).toBeTruthy();
  });
});
